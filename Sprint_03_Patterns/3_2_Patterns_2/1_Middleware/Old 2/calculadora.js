

// Declarar funció d'operacions
const suma = function(x, y){
    return x+y;
};
const resta = function(x, y){
        return x-y;
};
const multiplica = function(x, y){
        return x*y;
};

// Exporatar Funció
module.exports = {
    suma, resta, multiplica
}

