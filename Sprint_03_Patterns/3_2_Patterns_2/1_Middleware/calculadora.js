


class Calculadora {
    add({a, b}) {
      return a + b;
    }
    subtract({a, b}) {
      return a - b;
    }
    multiply({a, b}) {
      return a * b;
    }
  }


// Exporatar Funció
module.exports = Calculadora;
